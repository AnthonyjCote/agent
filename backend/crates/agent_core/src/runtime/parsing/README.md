# Parsing

Strict parsing of structured model outputs used by runtime stages.

Examples:
- Ack decision JSON parsing/validation.
- Structured tool envelope extraction helpers.

Keep parser behavior deterministic and side-effect free.
