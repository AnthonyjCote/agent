[package]
name = "agent_desktop"
version = "0.1.0"
edition = "2021"

[dependencies]
agent_core = { path = "../agent_core" }
agent_persistence = { path = "../agent_persistence" }
adapter = { path = "../adapter" }
serde_json = "1"
