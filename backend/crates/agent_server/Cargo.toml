[package]
name = "agent_server"
version = "0.1.0"
edition = "2021"

[dependencies]
agent_core = { path = "../agent_core" }
app_persistence = { path = "../app_persistence" }
adapter = { path = "../adapter" }
serde_json = "1"
